# HydraDB — required for all ingestion + recall
HYDRADB_API_KEY=

# LLM — OpenAI GPT-4o for extraction + synthesis agents
OPENAI_API_KEY=

# Twitter — optional; Phase 1 uses hand-pasted tweets in data/seed_tweets.yaml
TWITTER_BEARER_TOKEN=
